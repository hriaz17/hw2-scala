package csc583

class InvertedIndex(val inputFile: String) {
	def runQ8_1_1(query: String): Seq[Document] = {
		List()
	}

	def runQ8_1_2(query: String): Seq[Document] = {
		List() 
	}

	def runQ8_2(query: String): Seq[Document] = {
		List() 
	}
}